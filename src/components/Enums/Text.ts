export enum TextPosition {
  left = "text-left",
  center = "text-center",
  right = "text-right",
}

export enum MdTextPosition {
  left = "md:text-left",
  center = "md:text-center",
  right = "md:text-right",
}

export enum TextColor {
  black = "text-black",
  white = "text-white",
  green = "text-green",
  red = "text-red",
}
